import { defineConfig } from 'vite'

export default defineConfig({
  base: '/Interactive-Graphics_HW/star-wars/distNew/',
  // …any other config…
})
// vite.config.ts
// import { defineConfig } from 'vite'

// export default defineConfig(({ command }) => ({
//   base: command === 'serve'
//     ? '/'                           // local dev
//     : '/Interactive-Graphics_HW/star-wars/dist/',  // GitHub Pages sub-path
//   // …your other config…
// }))
